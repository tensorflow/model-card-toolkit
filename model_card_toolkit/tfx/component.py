"""
The ModelCardGenerator stub to issue removal notice.
"""

class ModelCardGenerator():
    """Component spec for the ModelCardGenerator."""
    raise NotImplementedError(
        """
        Component with all dependencies was move to the TFX Addons project (https://github.com/tensorflow/tfx-addons).
        The component and its dependencies are maintained by the TFX SIG project.

        Install the tfx addons package with

        ```
        $ pip install tfx-addons[model_card_generator]
        ```

        You can then import the component via
        ```
        from tfx_addons.model_card_generator.component import ModelCardGenerator
        ...
        ```

        and use it as usual.
        """
    )
